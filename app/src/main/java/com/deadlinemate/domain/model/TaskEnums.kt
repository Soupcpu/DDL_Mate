package com.deadlinemate.domain.model

enum class ImportanceLevel { HIGH, MEDIUM, LOW }
enum class UrgencyLevel { OVERDUE, HIGH, MEDIUM, LOW }
enum class TaskStatus { TODO, IN_PROGRESS, DONE, EXPIRED }
enum class TaskCategory { STUDY, HOMEWORK, COMPETITION, MEETING, LIFE, OTHER }
enum class RepeatRule { NONE, DAILY, WEEKLY, MONTHLY, CUSTOM }
